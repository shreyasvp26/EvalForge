[project]
name = "agent-eval-workers"
version = "0.0.0"
description = "Background workers for the Coding Agent Evaluation Platform (scaffold only)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "agent-eval-shared",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_eval_workers"]

[tool.uv.sources]
agent-eval-shared = { workspace = true }
