# @agent-eval/config

Shared ESLint, Prettier, and TypeScript configuration for the monorepo.

Consumers extend these configs from the workspace root and individual packages. No runtime logic lives here.
